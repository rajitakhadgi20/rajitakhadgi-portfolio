# URL of the backend API (see ../backend)
VITE_API_URL=http://localhost:4000
# URL of the admin dashboard (see ../admin) — used by the Login page to redirect after sign-in
VITE_ADMIN_URL=http://localhost:5174
